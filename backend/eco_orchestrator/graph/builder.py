# Compiling the DAG (LangGraph)
# TODO: use StateGraph to build wait-for-green -> resume flow

from .state import GraphState
from . import nodes


def build_graph():
    """Build and return the compiled LangGraph. Placeholder."""
    # from langgraph.graph import StateGraph
    # graph = StateGraph(GraphState)
    # graph.add_node("wait_for_green", nodes.wait_for_green)
    # graph.add_node("resume", nodes.resume)
    # ...
    return None
